NAME
        _printf - generates output based on a specified format

SYNOPSIS
        int _printf(const char *format, ...);
        ("String");
        ("%[c]...", 'data');
        ("%[s]...", "data");
        ("%[d]...", data(numerals));
        ("%[i]...", data(numerals));

